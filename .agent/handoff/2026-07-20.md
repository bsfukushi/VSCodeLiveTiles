# 2026-07-19〜20 セッション要約

> HANDOFF.md からのロールオーバー（2026-07-23 実施）。直前の要約: `2026-07-18.md`

## セッション一覧

- **2026-07-19 17:38** — Step 7-2 タイルのドラッグ並べ替え（v0.12.0）実装・検証・リリース
- **2026-07-20 06:00**（継続）— Step 7-4 右クリックメニュー拡張（v0.13.0）実装・検証・リリース

## 完了機能

### v0.12.0 タイルのドラッグ並べ替え
- 手動並びリスト `_order`（HWND キー・メモリ上）。整列を HWND 昇順 → リスト順に変更。
  新規は末尾 / 閉じたウィンドウの記憶は保持 / 死んだ HWND は上限 64 件で先頭側から間引く（`PruneOrder`）
- ドラッグ検知はグリッドの Preview（トンネル）段階で拾い、ドラッグした押下は MouseUp を
  `Handled` にしてクリック切替経路へ通さない（既存のクリック経路を無改造で温存）
- 半透明化: WPF の `Opacity` は DWM 合成サムネイルに効かないため `DwmThumbnail.UpdateDestination`
  に opacity 引数を追加。濃さは `ThumbnailTile.DragOpacity` に一本化
- キャンセル（Esc / 枠外ドロップ / キャプチャ喪失）は掴む前の位置へ復元

### v0.13.0 右クリックメニューの拡張
- メニュー: 最大化（トグル）/ レイアウト: 自動・縦1列・横1行（ラジオ3択）。**window モードのみ表示**
  （fullscreen は ceil(√N) を触らない方針 / ヒアリング確定）
- 擬似最大化: `WindowState.Maximized` は使わず作業領域へ `SetWindowPos`（枠なし最大化はタスクバーを覆うため）。
  解除で直前の矩形へ復帰、最大化中にリサイズされたら `SyncMaximizedState` で自動解除
- `window.json` に `maximized` / `restore*` / `layoutMode` を追加（既定値付きで旧形式も読める）

## 解決した問題
- Step 7-3 の明滅クロック延命（明滅中タイルがクローズで除去されると StopGlow 未呼び出し）→
  v0.12.0 のタイル除去パスで `SetCcState(CcState.None, ...)` を呼んで解消

## セッションハイライト
- **自動検証中に人が触ると観測が汚染される**: 「勝手に最大化」「勝手に並び替わった」を追っていたが、
  ともさんが同時に触っていた分だった。以降「触られていない状態での再現」を切り分け条件に追加
- **dev ビルドが黙って終了する**: 原因は多重起動防止ミューテックス（publish 版常駐中）。
  2 つ目のインスタンスはログを 1 行も残さず Shutdown する。dev 検証前には publish を止める
- **UI 検証はピクセル座標クリックをやめて UI Automation へ**: メニュー項目をピクセル座標で
  クリックして「終了」を 2 回踏んだ。MenuItem を名前指定して Invoke する方式に変えたら一発で安定
  （雛形: `scratchpad/menutest.ps1`）

## 次のセッションで最初にやること
1. ドラッグ並べ替え・新メニューの常用フィードバックを聞く（特に「ドラッグ直後のクリックが 1 回効かない」
   ことがないか — セッション終盤の同時操作で最終確認が取れていない唯一の経路）
2. Phase 6 積み残し: README（日英）にウィンドウモード / displayMode / 右クリックメニュー / ドラッグ並べ替えを追記
3. VSCode 派生対応（Insiders / Cursor / VSCodium）/ UI 文字列の英語化

## 注意点・ブロッカー
- 常駐は v0.13.0 に入れ替え済み（publish/ 運用）
- `bin/Release/net10.0-windows/appsettings.json` は fullscreen に戻してある
- window.json の layoutMode は検証の都合で `horizontal` のまま（常駐は fullscreen なので影響なし）
- dev ビルドを起動する前に publish 版を止めること（ミューテックスで黙って終了する）
- push が 403 になったら `gh auth switch --user bsfukushi`
