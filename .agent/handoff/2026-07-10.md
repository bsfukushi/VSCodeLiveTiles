# 2026-07-10 要約（セッション 5 本）

> 直前の要約: `.agent/handoff/2026-07-09.md`

## セッション一覧

| 時刻 | 概要 |
|---|---|
| 13:05 | GitHub 公開＋v0.7.3/v0.7.4 切り替え UX（フリーズ・ガチャつき解消） |
| 13:25 | 一般配布に向けた非機能監査 → TODO.md に Phase 4〜6 を策定 |
| 14:19 | v0.7.5 CC 状態バッジ堅牢化（死んだセッションの承認待ち固定＋PostToolUseFailure） |
| 15:00 | v0.8.0〜v0.8.3 フリーズ根治（例外ハンドラ・ログ・ウォッチドッグ→原因2つ特定） |
| 16:56 | v0.8.4/v0.8.5（ハンドル常時保持・モニター構成変化）→ Phase 4 完了 |
| 19:20 | v0.10.0 .NET 10 移行＋アイコン＋README 英語版 → Phase 5 完了 |

## 完了機能 / 解決した問題

- **GitHub 公開**: https://github.com/bsfukushi/VSCodeLiveTiles（bsfukushi 所有）。main＋全タグをプッシュ
- **v0.7.3/v0.7.4 切り替え UX**: 同期 Win32 呼び出しを非同期化（ShowWindowAsync / SWP_ASYNCWINDOWPOS / AttachThreadInput 廃止）＋現在地判定で不要な復元→移動→最大化をスキップ。「きれいにストレスなく切り替わる」とユーザー確認済み
- **v0.7.5 バッジ堅牢化**: `Resolve` に鮮度しきい値 SilentAge（1h・ハードコード）/ `Sweep()` を 1 分タイマーで時間駆動化 / post_tool_use_failure を settings.json＋フック＋MapState まで配線
- **v0.8.0〜v0.8.3 フリーズ根治**: 未処理例外ハンドラ＋ログ（`%LOCALAPPDATA%\VSCodeLiveTiles\logs\`）＋UI スレッドウォッチドッグ（Send/Input 分離）を先に整備 → 犯人 2 つ特定: ①SetForegroundWindow 同期呼び出し（Task.Run へ）②Drain() の events.jsonl 読み取り（42MB、追記直後 open だけで 140ms〜数秒 → I/O・パース全て背景スレッドへ）。A/B 実測で修正後 UI 停止 0 件
- **v0.8.4**: ハンドル常時保持（Drain のたびの open が 200ms〜19.9 秒に悪化していた。ローテーションはハンドル越しの長さ縮み＋FileSystemWatcher で検知）
- **v0.8.5**: DisplaySettingsChanged で再配置 / GetWidgetMonitor・GetTargetMonitor を null 返しにしモニター 0 枚クラッシュ経路封鎖 → **Phase 4 完了**
- **v0.9.0**（別セッション、TODO 反映済み）: CC フック同梱（hooks/append-event.mjs＋install.mjs）、events.jsonl 痩身（約 200B/行）＋5MB ローテーション。既定パス `~/.vscode-live-tiles/events.jsonl` に新設
- **v0.10.0**: .NET 10 LTS 移行（net10.0-windows・警告 0）/ アプリアイコン（icon.png→マルチサイズ ico）/ self-contained 単体 exe 62.1MB 検証 / README 英語版＋セキュリティ節＋実測リソース値 → **Phase 5 完了**

## セッションハイライト

- とも: 「タスクバーだときれい、タイルだとガチャつく」— 比較つき体感報告がそのまま原因特定の決め手に（対照実験が二分探索を省略）
- とも: 「このアプリケーションを広く VSCode で CC を動かしている人に使ってもらいたい」→ 一般配布へ方針転換。監査の最大の発見は「一番の差別化機能（バッジ）が CCPet 外部依存で一般環境で動かない」
- CC: PowerShell 計測 47ms → C# ハーネス 0.29ms。**測る道具を疑う**
- CC: ログ・ウォッチドッグを修正前に入れた判断が決定打（v0.8.1 の 37 秒後に 8.4 秒停止を捕捉、2 つ目の犯人発見）
- CC: v0.8.4 は**ユーザー報告より先にログが実害を教えた**（init-session ヘルスチェックで open 20 秒悪化を発見、Phase 5 項目を前倒し）
- とも: 「.NET10 への移行と合わせて、アプリのアイコン作ってみました」— アイコンが機能説明そのもの

## 次のセッションで最初にやること（19:20 セッションより）

1. 常用観察: v0.10.0（.NET 10）の安定性 / タスクバー・Alt+Tab の新アイコン表示 / ログに読み取り警告が出ていないか
2. Phase 6 に着手するなら本丸の**ウィンドウモード**（最前面・自由リサイズ・シングルモニター対応）の仕様詰めから。縦横比でのグリッド自動調整の設計が先

## 注意点

- 常駐は framework-dependent の publish/ 運用。単体 exe（243MB）は配布専用
- 旧アイコンが見えたら Windows のアイコンキャッシュ。放置で更新される
- push が 403 になったら `gh auth switch --user bsfukushi`
- note-articles は GitHub リモート切断済み（ローカルコミットのみ）
- TODO 残: `UpdateThumbnailRects` の DWM RPC 抑制（ウォッチドッグで実害観測してから）
